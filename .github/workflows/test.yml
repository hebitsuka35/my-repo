name: Annotation
on: push
jobs:
    log:
        runs-on: ubuntu-latest
        steps:
            - run: echo "::error::This is an error"
            - run: echo "::warning::W"
            - run: echo "::notice::N"